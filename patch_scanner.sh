sed -i -z 's|        if (!(window as any).MINDAR) {\n          const mindarCoreScript = document.createElement('"'"'script'"'"');\n          mindarCoreScript.type = "module";\n          mindarCoreScript.src = '"'"'https://cdn.jsdelivr.net/npm/mind-ar@1.2.5/dist/mindar-image.prod.js'"'"';\n          document.head.appendChild(mindarCoreScript);\n          await new Promise(resolve => { mindarCoreScript.onload = resolve; });\n        }\n\n        if (!(window as any).MINDAR?.IMAGE?.MindARThree) {\n          const mindarScript = document.createElement('"'"'script'"'"');\n          mindarScript.type = "module";\n          mindarScript.src = '"'"'https://cdn.jsdelivr.net/npm/mind-ar@1.2.5/dist/mindar-image-three.prod.js'"'"';\n          document.head.appendChild(mindarScript);\n          await new Promise(resolve => { mindarScript.onload = resolve; });\n        }\n\n        const { MindARThree } = (window as any).MINDAR.IMAGE;\n\n        // Compile target image locally if not a precompiled .mind file\n        // For simplicity in this demo, MindAR needs .mind file. We'"'"'ll use the compiler internally\n        const { Compiler } = (window as any).MINDAR.IMAGE;|        let MindARThree, Compiler;\n        try {\n          const core = await import("https://esm.sh/mind-ar@1.2.5/dist/mindar-image.prod.js");\n          const three = await import("https://esm.sh/mind-ar@1.2.5/dist/mindar-image-three.prod.js");\n          Compiler = core.Compiler || (window as any).MINDAR?.IMAGE?.Compiler;\n          MindARThree = three.MindARThree || (window as any).MINDAR?.IMAGE?.MindARThree;\n        } catch (e) {\n          console.error("ESM load failed", e);\n          throw new Error("Lỗi tải thư viện MindAR");\n        }|g' src/components/ARScanner.tsx
